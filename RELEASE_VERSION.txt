Maktabah / Salaf Library — Build Fix Release 2.7.1

Дата сборки: 2026-06-22

Исправлено:
- GitHub Actions падал из-за старого файла src/hooks/useData.ts, который импортировал fallbackBooks/fallbackAudio/fallbackBiographies/fallbackFawaid/fallbackCategories;
- добавлен совместимый src/hooks/useData.ts под текущий Zustand store;
- добавлены alias-экспорты fallbackBooks, fallbackAudio, fallbackBiographies, fallbackFawaid, fallbackCategories в src/data/*;
- это сохраняет совместимость со старыми файлами, если они остались в репозитории после ручной загрузки;
- сборка снова проходит успешно.

Проверка:
- npm run build: OK
- npm run check: OK
- npm audit moderate+: 0 vulnerabilities

Рекомендация:
- загрузить этот архив поверх текущего репозитория через GitHub Desktop;
- commit: Build fix 2.7.1;
- push;
- дождаться GitHub Actions.
