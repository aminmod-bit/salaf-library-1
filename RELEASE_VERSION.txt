Salaf Library — Build Fix Release 3.0

Дата сборки: 2026-06-25

Исправлено:
- GitHub Actions падал из-за старых тестовых файлов src/tests/*, которые требовали vitest и @testing-library/react;
- TypeScript build теперь исключает тестовые файлы: src/tests, *.test.ts, *.test.tsx, *.spec.ts, *.spec.tsx;
- добавлена совместимость store с устаревшим ThemeToggle.tsx:
  - export type ThemeMode = 'dark' | 'light' | 'system';
  - theme в Zustand store;
  - setTheme(theme);
- theme добавлен в persisted state;
- это сохраняет совместимость, если в репозитории остались старые ThemeToggle/tests после ручных загрузок.

Проверка:
- npm run build: OK
- npm run check: OK
- npm audit moderate+: 0 vulnerabilities

Commit message:
Build fix 3.0 theme and tests compatibility
